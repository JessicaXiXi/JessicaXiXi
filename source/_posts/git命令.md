### git命令

~~~
git clone git仓库地址 本地文件夹
git checkout src #切换分支
git branch #查看当前分支

git pull #拉取远程文件到本地
git status #查看本地改动

git add . #添加改动到暂存区
git commit -m "提交信息" #备注提交信息
git push #提交改动到远程仓库
~~~

